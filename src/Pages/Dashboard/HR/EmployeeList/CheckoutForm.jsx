import React from 'react';

const CheckoutForm = ({employeeName,salary,bank_account_no,selectedMonth,selectedYear}) => {
    return (
        <div>
            
        </div>
    );
};

export default CheckoutForm;